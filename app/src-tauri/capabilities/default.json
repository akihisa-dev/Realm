{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The minimal permissions required by the Realm desktop window.",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
