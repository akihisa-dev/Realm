interface:
  display_name: "Realm Dependency Update"
  short_description: "Update Realm Electron dependencies and evidence"
  default_prompt: "Use $realm-update-dependencies to update Realm Electron, Vite, and Node dependencies and evidence."
