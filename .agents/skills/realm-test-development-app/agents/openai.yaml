interface:
  display_name: "Realm App Test"
  short_description: "Verify the Realm Electron development app"
  default_prompt: "Use $realm-test-development-app to verify the Realm Electron development app when explicitly requested."
